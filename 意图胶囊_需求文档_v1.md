# 意图胶囊 (Intent Capsule) — 项目文档

> 最后更新：2026-06-01
> 状态：v1.0.0 已提交 Chrome Web Store 审核

---

## 一句话定位

一个 AI 对话页内悬浮工具：逐条勾选对话消息（默认只选你自己的），一键拼成带来源标记的干净上下文块，复制后粘到任意其他模型继续聊。

**别人是铲子（整段搬走），它是手术刀（你挑哪条带哪条）。**

---

## 已实现功能（v1.0.0）

### 核心交互
- **悬浮 FAB**：页面右侧常驻，鼠标悬停滑出，`Alt+C` 快捷键
- **逐条 checkbox**：每条消息旁注入复选框，支持任意单选/多选
- **全选我的**：一键选中全部用户消息，跳过 AI 回复
- **清空**：重置所有选择
- **一键复制**：带时间胶囊头的完整文本进剪贴板
- **AI 整理**：调 DeepSeek API 对消息做最小润色（完整保留每条，去废话/错别字）

### 时间胶囊头格式
```
【上下文交接 · 意图胶囊】
来源：与 {模型名} 的一段对话{《标题》}（{日期}），带过来 {N} 条消息
说明：以下是我（用户）说过的话，代表我的意图与需求，非 AI 回复。请基于此继续。
———
```

### 虚拟滚动支持（关键技术）
DeepSeek、豆包等平台视口外节点会被卸载，普通 DOM 选择只能拿到可见部分。解决方案：

**方案一：API 截获**（DeepSeek）
- MAIN world 脚本（`document_start`）拦截页面自身的 `history_messages` 请求
- 握手机制解决时序问题：content script 就位后主动索要，interceptor 重发缓存数据

**方案二：scrollAndCollect**（豆包 + DeepSeek 兜底）
- 点「全选我的」时从顶到底自动滚动
- 每滚一步立即读取当前可见节点存入内存（而非滚完再读）
- 用 `data-observe-row` 属性去重（豆包稳定 ID，经 Manus 实地验证）

### 支持平台（9 个）

| 平台 | 域名 | 消息选择器 | 角色检测 |
|------|------|----------|---------|
| Claude | claude.ai | `[data-testid="user-message"]` LCA | data-testid |
| ChatGPT | chatgpt.com | `[data-message-author-role]` | data-message-author-role |
| Gemini | gemini.google.com | `user-query, model-response` | tag name |
| Kimi | www.kimi.com | chatList 子节点 | CSS 对齐检测 |
| DeepSeek | chat.deepseek.com | `.ds-markdown` LCA | .ds-markdown 存在性 |
| 豆包 | www.doubao.com | `.v_list_row[data-observe-row]` | .bg-g-send-msg-bubble-bg / .justify-end |
| 通义千问 | www.qianwen.com | chatItem 子节点 | CSS 对齐检测 |
| 智谱 GLM | bigmodel.cn | chatList 子节点 | CSS 对齐检测 |
| MiniMax | agent.minimaxi.com | chat-list 子节点 | CSS 对齐检测 |

---

## 技术架构

### 文件结构
```
src/
├── background.ts              # Service worker：AI 整理 API 调用
├── content/
│   ├── index.ts               # 入口：适配器注册、面板初始化、SPA 路由监听
│   ├── ControlPanel.ts        # 主面板 UI 和交互逻辑
│   ├── CheckboxInjector.ts    # DOM 复选框注入和选择状态管理
│   ├── historyStore.ts        # postMessage 接收端，存储 API 截获的历史消息
│   ├── interceptor.ts         # MAIN world，拦截 fetch/XHR，提取历史消息
│   └── adapters/
│       ├── types.ts           # Adapter 接口定义
│       ├── registry.ts        # 按 hostname 匹配适配器
│       ├── claude.ts          # Claude 适配器
│       ├── chatgpt.ts         # ChatGPT / OpenAI 适配器
│       ├── gemini.ts          # Gemini 适配器
│       └── china.ts           # 国内模型适配器（Kimi/DeepSeek/豆包/Qwen/GLM/MiniMax）
options.html / src/options/    # 设置页（API Key、模型、自定义 Prompt）
```

### Adapter 接口
```typescript
interface Adapter {
  host: string
  sourceModelName: string
  messageSelector: string
  getMessages?(): Element[]                    // 自定义获取消息列表
  roleOf(node: Element): 'user' | 'assistant'
  textOf(node: Element): string
  mountPoint(): Element
  scrollContainer(): Element | null
  conversationTitle?(): string | null
  ensureAllLoaded?(): Promise<void>            // 滚动加载（ChatGPT）
  usesApiHistory?: boolean                     // 走接口截获（DeepSeek/豆包）
  collectMessages?(): Promise<{role, text}[]>  // 边滚边捕获（DeepSeek/豆包兜底）
}
```

### 全选我的执行优先级
```
① API 截获（usesApiHistory）→ 握手 requestApiHistory()
② scrollAndCollect（collectMessages）→ 边滚边捕获
③ DOM injector（仅可见节点）→ 兜底
```

### postMessage 安全机制
index.ts 在 DOM 上写入随机 nonce（`document.documentElement.dataset.icNonce`），interceptor 读取后附在每条广播上，historyStore 校验 nonce，防止页面 JS 伪造消息污染历史数据。

---

## 上线信息

| 项 | 内容 |
|---|---|
| 版本 | v1.0.0 |
| 提交日期 | 2026-06-01 |
| 开发者账号 | wjuan2314@gmail.com |
| 商店状态 | 审核中（预计 3-7 工作日） |
| 隐私政策 | https://gist.github.com/wgjuan2314/402bcf0f15827049e31a282a210932e4 |

---

## 待办（审核通过后）

### v1.0.1（安全修复，优先）
- [ ] 提交修复包（已准备好：`/Users/suansuan/intent-capsule-v1.0.0.zip` 包含所有修复）
- [ ] 修复内容：API key 移除硬编码、postMessage nonce 校验、deepseekRows `>=` 改 `>`、豆包单条消息 `>1` 改 `>0`、claude.ts 空节点过滤

### 功能待验证
- [ ] DeepSeek「全选我的」API 截获路径是否正常工作（用户未最终确认）
- [ ] 其他国产平台（Kimi/Qwen/GLM/MiniMax）实际测试

### 后续迭代方向（V2+）
- 自动注入到目标模型（V1 用剪贴板，V2 考虑直接注入）
- 跨对话搜索 / 收藏库
- 更多平台适配

---

## 研发过程关键决策记录

| 问题 | 方案 | 原因 |
|------|------|------|
| 豆包虚拟滚动 | scrollAndCollect | 无公开历史 API；Manus 实地调研确认 `[data-observe-row]` 选择器 |
| DeepSeek 虚拟滚动 | API 截获 + scrollAndCollect 兜底 | `history_messages` 接口带 PoW 鉴权，只能被动截获，不能主动调 |
| postMessage 安全 | nonce 校验 | Code Review 发现页面 JS 可伪造消息，nonce 是最小成本修复 |
| AI 整理截断问题 | max_tokens 4000→16000 + prompt 改为保留全部 | 原 prompt 要求"改写成一段"导致压缩，用户反馈不合理 |
| 角色检测 | CSS 对齐检测（margin-left:auto）兜底 | 国产模型 class 名哈希化，颜色检测不可靠，布局信号更稳定 |
